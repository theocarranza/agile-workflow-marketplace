# Host adapters

Optional packaging track that builds Cursor / Claude / Codex marketplaces from the flat Open Plugins root.

```bash
./scripts/validate.sh
./scripts/build_all.sh
```

Install with `adapters/<host>/install.sh` (or the curl one-liners in the root README).
