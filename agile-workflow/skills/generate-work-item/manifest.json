{
  "name": "generate-work-item",
  "description": "Context7 research, raw spec + Tickets/Ready draft (title, Requisitos, Critérios de Aceite), Azure create on approval.",
  "input_schema": {
    "type": "object",
    "properties": {
      "work_item_type": {
        "type": "string",
        "enum": ["epic", "feature", "user-story"],
        "description": "Azure DevOps work item type to generate"
      },
      "title": {
        "type": "string",
        "description": "Short work item title"
      },
      "description": {
        "type": "string",
        "description": "Problem statement or scope in the author's words"
      },
      "parent": {
        "type": "string",
        "description": "Parent work item id or URL (Epic for Feature, Feature for Story)"
      },
      "attachment": {
        "type": "string",
        "description": "Supporting documentation URL or vault path"
      }
    },
    "required": ["work_item_type", "title", "description"]
  },
  "output_signature": {
    "required": ["spec_path", "draft_path", "title"]
  }
}
